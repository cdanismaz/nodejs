function loginPage() {
    //this.errorMessage = element(by.css(""));

    this.errorMessage = {
        getText() {
            return new Promise((resolve, reject) => {
                

            });

        }
    }



}